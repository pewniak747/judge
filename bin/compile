#!/bin/bash

SRC_FILE=$1
PROGRAM_NAME=$2
TMP_DIR=tmp
CLEANUP=bin/cleanup

outfile=$TMP_DIR/$PROGRAM_NAME

echo "compiling [$PROGRAM_NAME]"
if [ ! -e $SRC_FILE ]; then
  echo "source not found!"
  echo
  exit 3
fi

g++ -o $outfile $SRC_FILE
